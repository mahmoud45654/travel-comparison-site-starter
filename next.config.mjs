/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Add any specific configurations here, e.g., images
  // images: {
  //   domains: ['example.com'], // Add domains for external images
  // },
};

export default nextConfig;